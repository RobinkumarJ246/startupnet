'use client';

const NotificationBell = ({ count }) => (
  <button className="relative p-2 text-gray-600 hover:bg-gray-100 rounded-full">
    {/* SVG and notification count */}
  </button>
);

export default NotificationBell;